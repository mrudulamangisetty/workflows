#SESSION 80
name: nodejs-feature-branch-workflow
on: 
  # some one should call this
  workflow_call:
    inputs:
      project:
        description: 'Project name'
        required: true
        type: string
      component:
        description: 'Component name'
        required: true
        type: string
       
env:
  project: ${{ inputs.project }}
  component: ${{ inputs.component }}
  acc_id: 833415032280
jobs:
  # Job name
  trigger:
    runs-on: self-hosted # Github hosted runners
    permissions:
      statuses: write
    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: read package.json version
        id: package_version
        run: |
          APP_VERSION=$(jq -r .version package.json) 
          echo "APP_VERSION=$APP_VERSION" >> $GITHUB_ENV

      - name: check app version
        run: echo "$APP_VERSION"

      - name: install dependencies
        run: |
           #rm -rf node_modules package-lock.json  
           #git add package.json package-lock.json
           npm install
      

      - name: unit tests
        id: unit_tests
        run: npm test

      - name: UpdateStatus — unit_tests
        # for custom actions no need to mention file name
        uses: mrudulamangisetty/workflows/.github/actions/update-commit-status@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }} # here github token is autogenerated
          context: unit-tests
          state: ${{ steps.unit_tests.outcome }} # unit_test from above job id
          description: Unit tests ${{ steps.unit_tests.outcome }}

      - name: sonar scan
        id: sonar_scan 
        uses: SonarSource/sonarqube-scan-action@v8.0.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL }} # or https://sonarcloud.io

      - name: SonarQube Quality Gate check
        id: sonar_gate
        uses: sonarsource/sonarqube-quality-gate-action@master
        with:
          pollingTimeoutSec: 600
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL }} #OPTIONAL

      - name: Update status — sonar-scan
        #if: always()
        uses: mrudulamangisetty/workflows/.github/actions/update-commit-status@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          context: sonar_scan
          state: ${{ steps.sonar_gate.outcome }}
          description: SonarQube quality gate ${{ steps.sonar_gate.outcome }} #from context 

      - name: Check Dependabot Alerts
        id: library_scan
        uses: Posture-Cybersecurity/dependabot-security-checker@v1.1.5
        with:
          github-token: ${{ secrets.DEPENDABOT_TOKEN }}
          severities: 'critical,high'  # Only check critical and high
          output-file: 'security-report.csv'
          fail-on-alerts: 'true'  # Fail workflow if alerts found

      - name: Update status — library-scan
        uses: mrudulamangisetty/workflows/.github/actions/update-commit-status@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          context: library-scan
          #state: ${{ steps.library_scan.outcome == 'success' && 'success' || 'failure' }}
          state: ${{ steps.library_scan.outcome }} 
          description: Library scan ${{ steps.library_scan.outcome }}
          


      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v6.1.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: build image
        id: build_image
        run: |
          docker build -t  ${acc_id}.dkr.ecr.us-east-1.amazonaws.com/${project}/${component}:${APP_VERSION} .
          
      - name: trivy scan
        id: trivy_scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.acc_id }}.dkr.ecr.us-east-1.amazonaws.com/${{ env. project }}/${{ env.component }}:${{ env.APP_VERSION }}
          scan-type: 'image'
          vuln-type: os
          format: 'table'
          exit-code: 1
          severity: 'CRITICAL,HIGH'

      - name: Update status — trivy-scan
        uses: mrudulamangisetty/workflows/.github/actions/update-commit-status@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          context: trivy-scan
          state: ${{ steps.trivy_scan.outcome }}
          description: Unit tests ${{ steps.trivy_scan.outcome }}

      - name: push image
        id: push_image
        run: |
          docker push  ${acc_id}.dkr.ecr.us-east-1.amazonaws.com/${project}/${component}:${APP_VERSION}

      - name: Update status — push-image
        uses: mrudulamangisetty/workflows/.github/actions/update-commit-status@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          context: push-image
          state: ${{ steps.push_image.outcome }}
          description: Unit tests ${{ steps.push_image.outcome }}

      - name: Slack alert
        if: always()
        uses: slackapi/slack-github-action@v3.0.3
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            text: "${{ job.status == 'success' && ':white_check_mark:' || ':x:' }} *${{ inputs.project }}/${{ inputs.component }}* feature pipeline *${{ job.status }}*"
            blocks:
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "${{ job.status == 'success' && ':white_check_mark:' || ':x:' }} *${{ inputs.project }}/${{ inputs.component }}* feature pipeline: *${{ job.status }}*\n*Branch:* `${{ github.ref_name }}`\n*Commit:* `${{ github.sha }}`\n*<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Run>*"

#----------------check dependabot file in runner
# cd /runner-data/actions-runner/_work/
# find / -name "security-report.csv" 2>/dev/null (find in all the directories)
# find /runner-data/actions-runner/_work/ -name "security-report.csv" 2>/dev/null ((find in _work directory))
# cd catalogue-unit-tests/catalogue-unit-tests
# cat security-report.csv

# ---------
# Better practice

# Add an upload step:

# - name: Upload security report
#   uses: actions/upload-artifact@v4
#   with:
#     name: security-report
#     path: security-report.csv

# Then you can download it directly from the workflow run page.

# Without this, the file stays on the runner and is easy to miss.
-------------------------
#Dependabot alerts are tied to your repository's committed code.
#So the only way to dismiss an alert is to fix it in the actual source files
 #  and push them to the main branch. Until that happens,
 # the alert stays open — no matter what you do in CI, locally, or anywhere else.
 ------------package-lock.json
 # cd /runner-data/actions-runner/_work/
 # cd catalogue-unit-tests/catalogue-unit-tests
 # ls -l
 # cat package-lock.json | grep "redis"
 # rm -rf node_modules package-lock.json
 # npm ls redis ---> shows version
 # git add package.json package-lock.json
 # npm install

